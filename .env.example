# API runtime
VAREMI_DATABASE_PATH=.var/varemi-go.db
VAREMI_SESSION_TTL_SECONDS=86400

# Keep 0 for local HTTP development. Production HTTPS must set this to 1.
VAREMI_SECURE_COOKIES=0

# Optional local validation mode: serve apps/web directly from FastAPI without Vite.
VAREMI_SERVE_WEB=0
